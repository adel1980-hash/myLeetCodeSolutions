class Solution {
    public int search(int[] nums, int target) {
        
        int temp = 0;
        for(int i =0;i<nums.length; i++)
        {
            temp = nums[i];
            if(target == temp)
            {
                return i;
            }
        }
        return -1;
    }
}
